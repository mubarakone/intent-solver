const fs = require('fs');
const path = require('path');
const sharp = require('sharp');

// This script is now only kept for reference
// We're using the existing storerunner-icon.png instead of generating new icons

console.log('Using existing storerunner-icon.png for PWA icons');
console.log('PWA icons setup completed successfully!'); 